export type Challenge = {
  title: string;
  description: string;
  createdDate: string;
};

export type ChallengeResponse = {
  title: string;
  description: string;
  createdDate: string;
};

export type NewChallenge = {
  title: string;
  description: string;
}