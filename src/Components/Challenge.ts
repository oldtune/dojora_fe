export type Challenge = {
  title: string;
  description: string;
  createdAt: string;
};

export type ChallengeResponse = {
  title: string;
  description: string;
  createdAt: string;
};

export type NewChallenge = {
  title: string;
  description: string;
}